{
  "name": "@se-2/polkadot-sdk",
  "packageManager": "yarn@3.2.3",
  "scripts": {
    "build": "cargo build --bin substrate-node --release && cargo build -p pallet-revive-eth-rpc --bin eth-rpc --release",
    "chain": "./target/release/substrate-node --dev",
    "rpc": "./target/release/eth-rpc --dev"
  }
}
